{
    "name": "background-crawler",
    "version": "1.0.0-alpha",
    "description": "A little tool to watch and process a folder and it's content.",
    "main": "main.js",
    "scripts": {
        "start": "electron .",
        "build:win": "electron-packager ./ background-crawler --platform=win32 --arch=ia32,x64 --asar --out=dist/ --overwrite",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Thomas Borck",
    "license": "ISC",
    "devDependencies": {
        "electron": "^9.4.0",
        "electron-packager": "^14.0.6"
    }
}
